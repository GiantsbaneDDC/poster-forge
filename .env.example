# PosterForge Configuration

# TMDB API Key (get one at https://www.themoviedb.org/settings/api)
TMDB_API_KEY=your_tmdb_api_key_here

# OMDB API Key (get one at https://www.omdbapi.com/apikey.aspx)
# Free tier: 1000 requests/day
OMDB_API_KEY=your_omdb_api_key_here

# Media folders to scan (comma-separated)
MEDIA_FOLDERS=/path/to/movies,/path/to/tv-shows

# Web UI port
PORT=8760

# Poster style options
POSTER_STYLE=badges
# Options: badges (corner badges), bar (bottom bar), minimal (small corner)

# Which ratings to show (comma-separated, max 3)
# Options: imdb, rt, metacritic, tmdb
RATINGS=imdb,rt,metacritic

# Overwrite existing poster.jpg files?
OVERWRITE=false

# Watch folders for new content?
WATCH_MODE=false
